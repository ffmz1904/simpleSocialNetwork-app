class Post {
  String userId;
  String title;
  String body;
  // List comments;
  // DateTime createdAt;
  // DateTime updatedAt;

  Post({
    this.userId,
    this.title,
    this.body,
  });
}
